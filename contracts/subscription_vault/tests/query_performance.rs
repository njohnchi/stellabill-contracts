//! Performance budget integration tests.
//!
//! This is a stub test target so the CI command
//! `cargo test -p subscription_vault --test query_performance` resolves. The
//! original performance suite was tied to the previous (broken) contract
//! implementation and was removed alongside it.

#[test]
fn perf_budget_smoke() {
    // [Perf] noop=0us
    println!("[Perf] noop=0us");
}
